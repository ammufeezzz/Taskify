import { PageClient } from "./page-client";

export const dynamic = 'force-dynamic';

export const metadata = {
  title: "Dashboard - Workedge",
};

export default function Dashboard() {
  return <PageClient />;
}
